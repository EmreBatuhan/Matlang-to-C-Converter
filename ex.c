#include "base.h" 
int main() { 
matrix M = createMatrix(1, 1);
matrix A = createMatrix(4, 4);
matrix z = createMatrix(3, 1);
matrix y = createMatrix(1, 1);
matrix x = createMatrix(1, 1);
matrix xy2 = createMatrix(4, 1);
float i;
float n;
float s;
float count;
float incr;
z.vals = {1, 1, 1};
print_matrix(M);
}